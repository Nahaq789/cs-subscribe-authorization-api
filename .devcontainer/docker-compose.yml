services:
  cs-subscribe-authorization-api-dev:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../:/app/authorization
    tty: true
    working_dir: /app/authorization
  
